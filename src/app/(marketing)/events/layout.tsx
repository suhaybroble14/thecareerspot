export const metadata = {
  title: "Events | The Spot",
  description:
    "Browse upcoming workshops, career panels, and community events at The Career Spot.",
};

export default function EventsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
