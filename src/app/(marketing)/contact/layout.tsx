export const metadata = {
  title: "Contact | The Spot",
  description:
    "Get in touch with The Career Spot. Send us a message, find our location, or connect with us on social media.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
