export const metadata = {
  title: "About | The Spot",
  description:
    "Learn about The Career Spot, a founder-led career hub in Bristol built around community and real career support.",
};

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
