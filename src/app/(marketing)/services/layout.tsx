export const metadata = {
  title: "Services | The Spot",
  description:
    "Explore our workshops, hot-desking, mentorship programme, and career events at The Career Spot.",
};

export default function ServicesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
