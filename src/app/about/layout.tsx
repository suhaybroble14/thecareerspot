export const metadata = {
  title: "About | The Spot — Work, Create, Connect",
  description:
    "Learn about The Career Spot — a founder-led, community-first career hub built for growth, not gatekeeping.",
};

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
